<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Result</title>
    <link rel="stylesheet" href="/styles.css" />
    <!-- CSS 파일 경로 -->
  </head>
  <body>
    <h1><%= title %></h1>
    <div>이름 : <%= userInfo.name %></div>
    <div>성별 : <%= userInfo.gender %></div>
    <div>
      생년월일 : <%= userInfo.year %>년 <%= userInfo.month %>월 <%= userInfo.day
      %>일
    </div>
    <div>관심사: <%= userInfo.interests.join(", ") %></div>
    <br />
    <br />
    <a href="/">홈으로 이동</a>
  </body>
</html>
